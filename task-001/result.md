### styles/globals.css
```css
/* globals.css */

/* Variables CSS */
:root {
    --color-bg: #000000; /* Fondo principal */
    --color-accent: #e53935; /* Color de acento */
    --font-primary: 'Inter', sans-serif; /* Fuente principal */
    --spacing-unit: 8px; /* Unidad de espaciado */
}
```

Este archivo `globals.css` contiene las variables CSS requeridas según los criterios de aceptación. Puedes copiarlo directamente en tu proyecto en la ruta `styles/globals.css`.